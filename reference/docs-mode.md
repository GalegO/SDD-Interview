# Documentation Ingestion Mode

Scan only readable documentation such as Markdown, text, and JSON. If the documentation volume is too large, request a narrower path. Use extracted facts only as drafts for user validation; never infer missing decisions. Pause with a focused question when sources conflict or omit a decision necessary for the active topic.

