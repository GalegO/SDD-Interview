# Open Knowledge Format (OKF)

When `okf_enabled` is true, every generated document begins with valid YAML frontmatter for type, title, timestamp, language, encoding, methodology, and tags. When false, omit frontmatter and preserve the template's document sections. OKF standardizes metadata for people and machines but adds visual overhead in plain-text editors.

