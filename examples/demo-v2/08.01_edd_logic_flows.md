---
type: "Ticket_Artifact"
title: "12_logic_flows"
timestamp: "2026-07-11T23:44:00-03:00"
language: "en-US"
encoding: "UTF-8 (No BOM)"
description: "Logical flow modeling and event-driven development (EDD)."
methodology: "EDD (Event-Driven Development)"
tags: ["ticket", "artifact", "12_logic_flows"]
---

# Logic Flows and Events (08.01_edd_logic_flows.md)

**Purpose:** Map the "Nervous System" of the application, defining the primary synchronous chain and how the system reacts asynchronously to domain events so as not to degrade the experience.

---

## 1. Main Sequence Flow (Synchronous)
> Mermaid diagram of the main flow (Happy Path).

The CLI architecture has a strictly synchronous and atomic ecosystem. To cover the entire I/O surface, we mapped individually the calls to interface commands and the base cycle of human interaction:

### 1.1 Command `/interview new <alias>`
```mermaid
sequenceDiagram
    actor User
    participant Agent
    participant FS as File System
    
    User->>Agent: Triggers `/interview new <alias>`
    Agent->>Agent: Injects base parameters into memory
    Agent->>FS: Creates physical `<alias>` directory
    Agent->>FS: Initializes clean `00.00_tracking_changelog.md`
    FS-->>Agent: I/O Confirmation
    Agent-->>User: Displays initial header and the 1st Question
```

### 1.2 Command `/interview docs <docs_path> <alias>`
```mermaid
sequenceDiagram
    actor User
    participant Agent
    participant FS as File System
    
    User->>Agent: Triggers `/interview docs <docs_path> <alias>`
    Agent->>FS: Recursively reads `<docs_path>` folder (Read-Only)
    FS-->>Agent: Returns legacy files
    Agent->>Agent: Injects legacy context into memory (Memory Limit)
    Agent->>FS: Creates `<alias>` directory
    Agent->>FS: Initializes `00.00_tracking_changelog.md`
    Agent-->>User: Displays header and the 1st Question anchored in context
```

### 1.3 Command `/interview resume <path>`
```mermaid
sequenceDiagram
    actor User
    participant Agent
    participant FS as File System
    
    User->>Agent: Triggers `/interview resume <path>`
    Agent->>FS: Reads `00.00_tracking_changelog.md` file
    FS-->>Agent: Returns YAML tree (Interview state)
    Agent->>Agent: Loads template corresponding to `current_topic_id`
    Agent-->>User: Renders next pending question on terminal
```

### 1.4 Standard Interaction (Answer Submission in Chat)
```mermaid
sequenceDiagram
    actor User
    participant Agent
    participant FS as File System
    
    User->>Agent: Types answer and submits in chat
    Agent->>Agent: Validates technical coherence and answer depth
    Agent->>FS: Writes structured knowledge into base artifact (e.g., `08.01_edd.md`)
    Agent->>FS: Updates covered topics list in `00.00_changelog.md`
    Agent-->>User: Returns Feedback for the answer and triggers Next Question
```

### 1.5 Command `/interview help`
```mermaid
sequenceDiagram
    actor User
    participant Agent
    
    User->>Agent: Triggers `/interview help`
    Agent->>Agent: Fetches commands dictionary in memory
    Agent-->>User: Displays list of commands and usability
```

### 1.6 Command `/interview status <path>`
```mermaid
sequenceDiagram
    actor User
    participant Agent
    participant FS as File System
    
    User->>Agent: Triggers `/interview status <path>`
    Agent->>FS: Reads `00.00_tracking_changelog.md`
    FS-->>Agent: Returns progress arrays
    Agent-->>User: Displays analytical dashboard on terminal (No I/O writes)
```

### 1.7 Command `/interview validate <path>`
```mermaid
sequenceDiagram
    actor User
    participant Agent
    participant FS as File System
    
    User->>Agent: Triggers `/interview validate <path>`
    Agent->>FS: Scans changelog and all generated artifacts
    Agent->>Agent: Executes structural audit and rule-breaking check
    Agent->>FS: Generates/Overwrites independent `00_validation_report.md` artifact
    Agent-->>User: Returns error/success balance in console
```

## 2. Asynchronous Flows and EDD (Event-Driven)
> Which actions are triggered by events (in background) so as not to lock the user's main flow?
- **Pass-Through (N/A) - Purely Synchronous Architecture:** Honoring the architectural definitions of determinism, the tool has no background processes or concurrent executions (Parallel threads). The entire execution and processing flow is strictly linear and blocking. Since File System writing operates on the order of milliseconds, introducing asynchronous events would not bring performance gains and would only add unjustifiable complexity to the engine.

## 3. Messaging Strategy
> What is the adopted messaging infrastructure / routing?
- **Pass-Through (N/A):** Due to the intrinsically local, synchronous, and atomic nature of the application, the adoption of message buses (Message Brokers like Kafka, RabbitMQ, or SQS) is dispensed with. CLI commands are ingested and routed directly in RAM memory by the LLM engine to the File System, removing messaging overhead from the architecture.

## 4. Caching Strategy
> How and where do we use Cache strategies? Invalidation rules.
- **Pass-Through (N/A):** The ecosystem is stripped of any dedicated in-memory Caching mechanism (like Redis or Memcached). With each interaction, the machine restores state by actively re-reading the File System (`00.00_tracking_changelog.md`) from absolute zero. Rather than an architectural flaw, this absence of cache is a vital feature: it forces an "absolute determinism" where the static file on the HDD is the only possible source of truth, nipping in the bud the classic risk of phantom state or cache desynchronization.
